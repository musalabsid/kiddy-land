import { mkdtemp, rm } from "node:fs/promises";
import { join } from "node:path";
import { tmpdir } from "node:os";
import { afterEach, describe, expect, test } from "bun:test";
import { createHostRuntime } from "../src/supervisor.ts";
const runtimes: Array<Awaited<ReturnType<typeof createHostRuntime>>> = [];
afterEach(async () => { for (const runtime of runtimes.splice(0)) await runtime.stop(); });
describe("reports HTTP", () => { test("requires Owner and serves JSON plus exports", async () => { const dir = await mkdtemp(join(tmpdir(), "kiddy-reports-http-")); const runtime = createHostRuntime({ dataDir: dir, port: 43146 }); runtimes.push(runtime); await runtime.start(); const base = runtime.server.url; expect((await fetch(`${base}/reports/financial`)).status).toBe(403); const invite = await (await fetch(`${base}/pairing/invitations`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ origin: base }) })).json() as { token: string }; const pair = await (await fetch(`${base}/pairing/redeem`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ token: invite.token, mode: "Owner Dashboard" }) })).json() as { device: { id: string } }; const login = await (await fetch(`${base}/auth/login`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ deviceId: pair.device.id, username: "owner", password: "change-me" }) })).json() as { token: string }; const headers = { authorization: `Bearer ${login.token}` }; const report = await fetch(`${base}/reports/financial?from=2024-01-01&to=2024-01-01`, { headers }); expect(report.status).toBe(200); expect((await report.json()).filters).toMatchObject({ from: "2024-01-01", to: "2024-01-01" }); const csv = await fetch(`${base}/reports/financial.csv?from=2024-01-01&to=2024-01-01`, { headers }); expect(csv.status).toBe(200); expect(csv.headers.get("content-type")).toContain("text/csv"); const pdf = await fetch(`${base}/reports/financial.pdf?from=2024-01-01&to=2024-01-01`, { headers }); expect(pdf.status).toBe(200); expect(pdf.headers.get("content-type")).toContain("application/pdf"); await rm(dir, { recursive: true, force: true }); }); });
