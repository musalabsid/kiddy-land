# Members & Cards

- Search members with one field (phone masked, max 5).
- Register: phone must `08` + >=10 digits.
- **Membership Cards:** Owner → Members → Print → CODE128 barcode, forced white/black, printable.

Package soft archive via dialog — archived hidden from Cashier.
