# Backups & Restore

Owner → **Backups**: auto daily, keep 10, latest age shows `just now/m/h/d` + size.

- **Restore:** dialog needs typing `RESTORE {id}` → confirms consequences.
- **Delete:** dialog lists consequences, no native prompt.
- **Never delete staged backup.**

If you see recovery blocked, clear it first.
